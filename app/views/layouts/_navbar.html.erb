<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" >GitInsight</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <% if current_user %>
          <%= link_to "Profile", profile_link_path(current_user), class: "nav-link" %>
        <% end %>
      </li>
    </ul>
    <% if current_user && current_user.user_name.nil? %><p class="pt-3 pr-2 text-danger">You are not a Github User</p><% end %>
    <form class="form-inline my-2 my-lg-0 position-relative">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" id="search-input">
      <div id="search-results" class="position-absolute"></div>
    </form>

    <% if user_signed_in? %>
      <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: 'mx-3 btn btn-primary'%>
    <% end %>
  </div>
</nav>
